# NIBRAS_AUDIT.md — تدقيق المنصة القانونية المغربية (SEO · AdSense · UX)

- **الإصدار**: 1.0
- **التاريخ**: 2026-09-14
- **النطاق**: المرحلة 1 من خطة التحول الكامل (35 مرحلة) — بدون إعادة كتابة؛ الحفاظ الكامل على الوظائف.
- **رمز الوثيقة**: NB-AUDIT-01
- **المكان**: `nibras-backend/`

---

## 1. ملخص تنفيذي

المنصة ناضجة تقنيًا وجاهزيتها للإعلانات (AdSense) شبه مكتملة: SSR لصفحات أساسية، sitemaps
ديناميكية مقسّمة، robots.txt ديناميكي يسمح بزحف AdSense، صفحات ثقة كاملة (من نحن، اتصل بنا،
الخصوصية، الشروط، الكوكيز، إخلاء المسؤولية، الدليل) كلها SSR، شريط موافقة كوكيز حقيقي بفئات
وإدارة، ووقف تحميل الإعلانات إلى حين الموافقة.

أكبَر الفجوات المؤثرة في SEO:
1. **تناقض النطاق**: وسوم `canonical`/`og` الثابتة في `index.html` على `https://nibraslaw.com` بينما
   الموقع الحي `nibras-law-platform.vercel.app` — وبما أن بلا JS HTML يُخدم لمعظم المسارات عبر
   SPA fallback، تُرسل إشارة canonical خاطئة للزاحف.
2. **الربط الداخلي كله عبر `#/...`** (fragments): لا تنقل الروابط قوة التصنيف ولا تكشف الصفحات
   للزاحف عبر الروابط الداخلية (المسارات الحقيقية مدعومة في الـ router فاخترناها).
3. **المدونة والمقالات بلا SSR** (نموذج كامل لكن المحتوى لا يصل للزاحف كـ HTML).
4. **مولّد العقود (1945 قالبًا) بلا صفحات تفصيلية SSR** — أعلى فرصة محتوى بحثية (وكالة، كراء، عمل...).

الخطة: معالجة ما دون كسر، مرحلةً مرحلة، مع اختبارات لكل مرحلة وتقرير SEO نهائي.

---

## 2. البنية وقياسات المحتوى

| المكوّن | القياس المُستخرَج (nibras.db المحلية) |
|---|---|
| نصوص قانونية (`legal_texts`) | **6,806** |
| فئات (`categories`) | 8 |
| مجالات قانونية (`legal_domains`) | 10 |
| اجتهادات (`jurisprudence`) | **16,587** |
| فئات الاجتهادات | 29 |
| مساطر (`procedures`) | 153 |
| خطوات المساطر (`procedure_steps`) | 657 |
| مقالات مدونة (`blog_articles`) | 3 (منها ما هو published) |
| فئات المدونة (`blog_categories`) | 10 |
| مستخدمون | 26 |
| مستأجرون (`tenants`) | 2 |
| قوالب الوثائق/العقود (فهرس المولد) | 1,945 (من `/api/generator/health`) |
| النخبة المحمّلة مسبقًا (`field_profiles.json`) | 48 عقدًا/وثيقة |

### خريطة العرض
- **الخادم**: Flask (Vercel serverless، `wsgi.py`) — DB تُحمَّل من `nibras_prod.db.gz` أو تُنزَّل.
- **الواجهة**: SPA متعددة الملفات في `frontend/` (index.html + assets/js modulized + PWA + i18n).
- **التوجيه**: `router.js` يدعم المسارات الحقيقية (History API) ويحوّل `#/old` تلقائيًا إلى مسارات حقيقية.
- **SSR موجود**: `app/seo.py` — laws، jurisprudence، procedures، قوائمها، domains، generator،
  about/contact، privacy/terms/cookie/disclaimer/guide.
- **Sitemaps**: `/sitemap.xml` (index) + `/sitemaps/{main,laws,jurisprudence,procedures}.xml`.
- **robots.txt**: ديناميكي (ويحقن النطاق الحي في رابط sitemap).

---

## 3. الأصول القوية — يُحافَظ عليها ولا تُعاد كتابتها

1. **SSR للصفحات الديناميكية الثلاث الكبرى** مع محتوى HTML كامل، 404 حقيقي (`noindex`) لا soft-404.
2. **Sitemaps من قاعدة البيانات** (لا ملف يدوي)، مقسّمة، مع index.
3. **روبوت ديناميكي** يسمح بوضوح بـ `Mediapartners-Google` و`Google-Display-Ads-Bot`.
4. **صفحات الثقة** مكتملة وSSR (أساس المراجعة الآلية AdSense ✓).
5. **شريط موافقة حقيقي** (`cookie-consent.js`): ضروري/وظيفي/تحليلي/إعلاني + شاشة إدارة + تخزين
   موافقة؛ و`ads.js` لا يحمّل الإعلانات إلا بعد موافقة `advertising` ✓.
6. **رؤوس أمنية**: nosniff، X-Frame-Options=DENY، Referrer-Policy، Permissions-Policy، HSTS شرطي.
7. **Cache-Control محسوب**: no-store للحساس + s-maxage للثوابت.
8. **i18n كامل عربي/فرنسي** (فئات `i18n.js`/`i18n-fr.js`).
9. **JSON-LD**: LegalService+WebSite ثابت، WebSite+BreadcrumbList ديناميكي، Article/WebPage لكل SSR.
10. **SearchAction + FTS5**، مع تطبيع عربي (المرحلة D-032).
11. **CSP** يسمح فقط بنطاقات Google المعلنة — لا نصوص عشوائية.
12. **اختبارات**: `tests/test_seo.py` وغيره — بوابة `scripts/run_checks.py` (ruff + pytest)، آخر بوابة معروفة 544 اختبارًا.

---

## 4. الفجوات والمشاكل (مرتّبة بالخطورة)

| # | الخطورة | المشكلة | الملفات المعنية | الأثر | الحل المقترح |
|---|---|---|---|---|---|
| **P1** | عالية | تباين النطاق: canonical/og:url/og:image هاردركود `nibraslaw.com` بينما الحي `nibras-law-platform.vercel.app` | `frontend/index.html` | إشارة canonical خاطئة للزاحف على كل صفحات SPA (بلا JS) | سكربت head صغير يعيد كتابة canonical/og حسب `location.origin` |
| **P2** | عالية | كل الروابط الداخلية `#/...` (تنقل، فوتر، tabbar، روابط فئات) — لا تمرير لينك للزاحف | `index.html`, `views/*.js` | قوة التصنيف والاكتشاف | تحويل الروابط الوظيفية إلى مسارات حقيقية `/about`… مع بقاء دعم `#/old` |
| **P3** | عالية | مقالات المدونة بلا SSR (التفصيل والقوائم) | `app/seo.py`, `routes/blog.py`, `views/blog.js` | المحتوى الطويل الوحيد الآمن لا يُفهرس كمحتوى | SSR للقائمة + التفصيل + `BlogPosting` JSON-LD + `blog.xml` |
| **P4** | متوسطة-عالية | قوالب المولد (1,945) بلا صفحات تفصيلية SSR | `generator.py`, `seo.py`, `views/generator.js` | فرصة بحثية مهدرة (وكالة/كراء/عمل/بيرميسيون) | صفحة تفصيل لكل قالب SSR + `documents.xml` |
| **P5** | متوسطة | `frontend/sitemap.xml` ملف ثابت قديم/تالف (URL vercel.app) وقد يصطدم الوهمي | `frontend/sitemap.xml` | ملف ميت/مضلل | حذف الملف؛ الـ route الديناميكي `/sitemap.xml` يغطي |
| **P6** | متوسطة | `sitemap_main` بـ `lastmod` ثابت؛ لا sitemap للمقالات/العقود؛ sitemaps بلا `lastmod` للقوائم الكبيرة | `seo.py` | تحديثات متكررة لا تصل Google | lastmod ديناميكي من DB + sitemaps جديدة |
| **P7** | متوسطة | SearchAction يوجّه إلى `/#/library/q/{term}` | `seo.js` (+ `index.html` JSON-LD) | رابط دايناميك بحث غير قابل للفهرسة العدّ | مسار حقيقي `/library/q/{term}` |
| **P8** | متوسطة | `/home` و`/` ضعف (كلاهما index.html)؛ canonical يطبع `/` والروابط تستخدم `/home` | `router.js, seo.py, index.html` | ازدواجية طفيفة | 301 `/home`→`/` أو توحيد الروابط |
| **P9** | متوسطة | لا hreflang ar/fr رغم وجود ترجمة كاملة | `index.html`, `seo.py` | غياب إشارة لغة إضافية | إضافة alternates للصفحات الثابتة (SSR) |
| **P10** | منخفضة-متوسطة | أوصاف نمطية مولّدة في SSR (لا تفرّد القانون) | `seo.py` | CTR أقل | دمج `official_ref`/المجال في الوصف |
| **P11** | منخفضة | `og-cover.png` واحد لكل الصفحات | `index.html, seo.py` | إشارة بصرية مكررة | صور حسب النوع (اختياري) |
| **P12** | منخفضة | `_list_procedures` JOIN وهمي `ON 1=0` | `seo.py:442` | لا أثر وظيفي؛ نظافة | حذفه |
| **P13** | منخفضة-متوسطة | لا ربط داخلي قانون↔اجتهاد/مسطرة حسب الفئة | `seo.py` | عجلة لينك ضعيفة بين المحتوى الكبير | إضافة قسم "ذات صلة" مختلط |

---

## 5. جاهزية AdSense — قائمة تحقق

| البند | الحالة |
|---|---|
| سكربت AdSense + `google-adsense-account` في head | ✅ موجود |
| `ads.txt` يخدم عبر Vercel (رؤوس vercel.json) | ✅ |
| `robots.txt` يسمح بـ `Mediapartners-Google` + `Google-Display-Ads-Bot` | ✅ |
| صفحات «من نحن» و«اتصل بنا» | ✅ SSR |
| سياسة خصوصية/شروط/كوكيز/إخلاء مسؤولية/دليل | ✅ SSR |
| شريط موافقة كوكيز حقيقي + إدارة تفضيلات + امتناع الإعلانات قبل الموافقة | ✅ |
| 404 حقيقي (لا soft-404) لتجنّب عقوبات الجودة | ✅ |
| محتوى كافٍ وفريد (نصوص/اجتهادات/مساطر) | ✅ (يُضاف: SSR المدونة والعقود لرفع الجودة المدركة) |
| سجل تحريري/مصادر رسمية (ميثاق) | ✅ (صفحات الثقة تشرح المصادر) |

الخَلَل الوحيد المؤثر على AdSense هو الجودة المدرَكة للصفحات SPA والتناقض النطاقي (P1) —
معالجتهما أولوية.

---

## 6. خريطة المراحل الثلاث والثلاثين (35) ← الحالة والملفات

> الترقيم فراغي يحترم خطة المستخدم؛ تُضاف بعد كل مرحلة: اختبار + فحص حي + (عند النشر) دفع.

| المرحلة | العنوان (حسب خطة المستخدم) | الحالة | الملفات الرئيسية |
|---|---|---|---|
| 1 | التدقيق الكامل | ✅ **هذه الوثيقة** | — |
| 2 | Technical SEO الأساس: توحيد النطاق + روابط حقيقية + سلب stub | 🔄 قادمة | `index.html`, `router.js`, حذف `frontend/sitemap.xml` |
| 3 | Sitemaps/robots ديناميكية كاملة + lastmod | فراغ | `seo.py`, `__init__.py` |
| 4 | صفحات الثقة وتطويرها (من نحن/اتصل/الخصوصية) + ربط | فراغ | `seo.py` |
| 5 | Footer/Header مؤسسي محترف + بيانات تواصل + شهادات | فراغ | `index.html`, `ui.js` |
| 6-… | (حسب الخطة: الرئيسية، المدونة + SSR + BlogPosting + FAQPage، مولد العقود + صفحات القوالب، أدوات الحاسبات/المقارن، محتوى موثق، قياس+أداء+أمن، A11y/404/بحث، تقرير SEO) | فراغ | `app/seo.py`, `views/*.js`, `tests/` |

---

## 7. خطة التنفيذ المقترحة (الدفعات الآمنة)

- **الدفعة أ (فورية، أثر سريع، خطر منخفض)**: إصلاح P1/P2/P5/P8 — سكربت canonical ديناميكي، تحويل
  روابط الواجهة إلى مسارات حقيقية، حذف stub sitemap، 301 `/home`→`/`.
- **الدفعة ب**: P3/P4 — SSR المدونة + صفحات القوالب + sitemaps جديدة (blog/doc + lastmod ديناميكي).
- **الدفعة ج**: P6/P7/P9/P10/P13 — تحسينات sitemaps/SearchAction/hreflang/أوصاف/ربط داخلي.
- **الدفعة د**: قياس وأداء (Core Web Vitals، تحقق Search Console)، تجارب إضافية للإعلان، UX/A11y، ثم
  `NIBRAS_SEO_REPORT.md`.

كل دفعة تنتهي بـ: رفّ نظيف، اختبارات ناجحة، نشر، تحقق حي، ثم دفع.

---

## 8. المقاييس والرصد المقترح (تُسجَّل في التقرير النهائي)

- فهرس الـ URL في Search Console (قبل/بعد) وفق الشرائح: laws / jurisprudence / procedures / blog / generator.
- نسبة الصفحات المفهرسة مقابل المرسلة في sitemaps.
- متوسط موضع الاستعلامات للكلمات الموجودة: "نص قانوني"، "عقد كراء"، "وكالة"، "اجتهاد قضائي"، اسم المنصة.
- CTR ووقت المكوث المقدر عبر Search Console.
- CWV (LCP/INP/CLS) على صفحات SSR والداخلية.

---

## 9. المخاطر والمحاذير

- **لا تقع في soft-404 / صفحات فارغة**: أي صفحة SSR يجب أن تعيد 404 حقيقي غياب المحتوى.
- **لا تُخترع بيانات قانونية**: أي تحسين محتوى يعتمد حقلًا موجودًا فعلًا في DB أو metadades قالبية.
- **لا تكسر العزل multi-tenant** في استعلامات sitemaps (مُقيَّدة بمستأجر الطلب كما في الوحدات).
- **لا تُبطئ الصفحة**: SSR جديد يجب أن يظل خفيفًا (استعلامات مفهرسة، حدود).
- **الموافقة**: أي أداة قياس خارجية جديدة (GA4…) لا تُضاف دون موافقة صريحة — تُوثَّق كخيار في التقرير.