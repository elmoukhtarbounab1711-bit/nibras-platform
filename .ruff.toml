target-version = "py310"

[lint]
# select (not extend-select) pins the exact rule set so newer ruff
# versions with expanded defaults do not change lint results; E501 is
# intentionally ignored (Arabic text/URLs exceed line length).
select = ["E", "F", "I", "W"]
ignore = ["E501"]
